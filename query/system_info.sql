SELECT
  computer_name,
  cpu_brand,
  cpu_logical_cores,
  cpu_type,
  hardware_model,
  hardware_serial,
  hardware_vendor,
  hardware_version,
  hostname,
  local_hostname,
  physical_memory,
  uuid
FROM system_info
